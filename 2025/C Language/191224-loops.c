#include <stdio.h>

int main(){

    for (size_t i = 1; i <= 10; i++)
    {
        printf("%d\n", i);
    }

    int limit = 0;
    while (limit <= 10)
    {
        printf("%d\n", limit);

        limit++;
    }
    
    

    return 0;
}